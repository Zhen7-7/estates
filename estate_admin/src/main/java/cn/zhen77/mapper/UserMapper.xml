<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.zhen77.mapper.UserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="cn.zhen77.pojo.User">
        <id column="userid" property="userid" />
        <result column="username" property="username" />
        <result column="phone" property="phone" />
        <result column="realname" property="realname" />
        <result column="identity" property="identity" />
        <result column="address" property="address" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        userid, username, phone, realname, identity, address
    </sql>

    <select id="selectAllUserinfo" resultType="cn.zhen77.pojo.User">
        select * from user;
    </select>
</mapper>
