<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.zhen77.mapper.EmpMapper">
    <select id="selectAll" resultType="cn.zhen77.pojo.Emp">
        select * from emp
    </select>
    <insert id="insertEmp" parameterType="cn.zhen77.pojo.Emp">
        insert into emp valuse(#{id},#{name},#{photo})
    </insert>
</mapper>